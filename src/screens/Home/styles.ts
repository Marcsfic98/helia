import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width:"100%"
  },
  userName:{
    paddingTop:30,
    paddingBottom:30,
    color:"#fff",
    fontSize:26,
    fontWeight:800
  }
});